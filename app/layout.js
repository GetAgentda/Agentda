// This is a placeholder file to satisfy Vercel's directory structure requirements
// The actual application is in the agentda directory as specified in vercel.json

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  );
} 